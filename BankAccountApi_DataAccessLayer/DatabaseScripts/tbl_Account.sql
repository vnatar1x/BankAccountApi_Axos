CREATE TABLE tbl_Account (
    AccountID int,
    Category int,
    Balance varchar(255),
    Status varchar(255),
    InterestRate varchar(255),
	CreatedDateTime datetime,
	ModifiedDateTime datetime
);